var Directives = function(node,value){
	switch(type){
		case text:
			this.vText(node,value)
	}
	
}

Directives.prototype.handle = function(mType){
	
}

Directives.prototype.vText = function(node, value){
	node.textContent != value && node.textContent == value;
}